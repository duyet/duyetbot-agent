version: '3.8'

services:
  agent-server:
    build:
      context: .
      dockerfile: infrastructure/docker/agent-server.Dockerfile
    ports:
      - "3000:3000"
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - WS_PORT=8080
    restart: unless-stopped
