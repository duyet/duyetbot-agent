# Local Development Environment Variables
# Copy this file to .env.local and configure your values

# Wrangler dev server port (default: 8787)
WRANGLER_PORT=8787

# Cloudflare configuration (for local wrangler dev)
# These are only needed if you want to use production Cloudflare services locally
# Otherwise, wrangler dev --local runs everything locally

# AI Gateway Configuration
# AI_GATEWAY_API_KEY=your_api_key_here

# GitHub OAuth (for authentication)
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret
