# Memory MCP Configuration
# URL of the memory-mcp service (use localhost for development, or deployed URL)
NEXT_PUBLIC_MEMORY_MCP_URL=https://memory-mcp.duyetbot.workers.dev

# Auth token for memory-mcp API (Bearer token authentication)
# Generate this by calling POST /api/authenticate with your GitHub token
NEXT_PUBLIC_MEMORY_AUTH_TOKEN=your-auth-token-here
