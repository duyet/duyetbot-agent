name = "duyetbot-memory"
main = "src/index.ts"
compatibility_date = "2024-11-20"

[vars]
ENVIRONMENT = "production"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "duyetbot-memory"
database_id = "placeholder-will-be-set-after-creation"

# Vectorize binding (for semantic search)
# [[vectorize]]
# binding = "VECTORIZE"
# index_name = "duyetbot-memory-index"

[env.staging]
name = "duyetbot-memory-staging"
[env.staging.vars]
ENVIRONMENT = "staging"

[env.development]
name = "duyetbot-memory-dev"
[env.development.vars]
ENVIRONMENT = "development"
