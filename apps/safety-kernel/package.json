{
  "name": "@duyetbot/safety-kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Immutable safety kernel for duyetbot - health checks, rollback, dead man's switch",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:branch": "wrangler versions upload --message=\"Branch deploy: $(git branch --show-current)\" --tag=$(git branch --show-current)",
    "config": "bun ../../scripts/config.ts safety",
    "test": "echo No tests",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
