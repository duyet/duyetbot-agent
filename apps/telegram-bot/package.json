{
  "name": "@duyetbot/telegram-bot",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --passWithNoTests",
    "type-check": "tsc --noEmit",
    "webhook:set": "bun scripts/webhook.ts set",
    "webhook:info": "bun scripts/webhook.ts info",
    "webhook:delete": "bun scripts/webhook.ts delete",
    "webhook:config": "bun scripts/webhook.ts config"
  },
  "dependencies": {
    "@duyetbot/prompts": "workspace:*",
    "agents": "^0.2.23",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.91.0"
  }
}
