description: "Response quality evaluation with production prompts"

prompts:
  - file://../prompts/telegram-prompt.cjs
  - file://../prompts/github-prompt.cjs

providers:
  - id: openrouter:x-ai/grok-4.1-fast
    label: grok-4.1-fast

tests: file://../datasets/quality-cases.yaml
outputPath: ../results/quality-results.json

evaluateOptions:
  maxConcurrency: 3
  showProgressBar: true

defaultTest:
  options:
    timeout: 60000
